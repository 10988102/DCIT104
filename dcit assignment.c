
#include <iostream>

using namespace std;

int main() {
    // listing all integers to be used
    // givennum id the number to be input
    int num, a, giennum, sum - 0;

    //Enter the end number of the sum of the sum of prime numbers
    cout <<"Find the sum of prime number upto : ";
    cin >> givennum;

        for(num =2; num <=givemmum; num++) {
                for(a = 2; a <= (num / 2); a++){
                        if(num % a == 0) {
                                a = num;
                                 break;
                        }
                }
        }

        //Adding of number when prime
        if(i != num) {
                sum +=num;

        }
}
cout << enl << "sum of prime number upto" <<givennum <<":" << sum;

return 0
//Saeed Hierah developed this program
// ID:10988102




